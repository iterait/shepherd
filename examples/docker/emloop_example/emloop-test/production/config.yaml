dataset:
  class: examples.docker.emloop_example.dummy.PostProcessDataset


model:
  name: Dummy
  class: examples.docker.emloop_example.dummy.DummyModel

eval:
  production:
    model:
      factor: 10
    dataset:
      post_process_factor: 2
